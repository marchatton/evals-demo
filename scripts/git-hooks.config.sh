# Repo-local config for `.agents/hooks/git/*`.
# Sourced by hooks; keep values "real defaults" (no stubs).

# Conventional Commits enforcement (commit-msg hook). 1 = enforce, 0 = skip.
ENFORCE_CONVENTIONAL_COMMITS=1

# Space-delimited branch names (pre-push hook warns on direct push).
GIT_HOOKS_PROTECTED_BRANCHES="main"

